{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Example fastapi",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "app.main:app",
                "--reload",
            ],
            "jinja": true,
            "envFile": "${workspaceFolder}/.env.local",
            "preLaunchTask": "alembic upgrade"
        }
    ]
}